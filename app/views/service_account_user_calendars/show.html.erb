<div class="row">
  <div class="col-xs-8">
    <h2><%= @calendar.calendar_name %> - Events</h2>
  </div>

  <div class="col-xs-4 text-right">
    <%= link_to "Create Event", new_service_account_user_calendar_event_path(params[:service_account_user_id], @calendar.calendar_id), { class: "btn btn-primary" } %>
  </div>
</div>

<table class="table table-striped table-hover">
  <thead>
  <tr>
    <th>Event Summary</th>
    <th>Start</th>
    <th>End</th>
  </tr>
  </thead>

  <tbody>
  <% @events.each do |event| %>
      <tr>
        <td>
          <%= event.summary %>
        </td>
        <td>
          <%= event.start.time %>
        </td>
        <td>
          <%= event.end.time %>
        </td>
      </tr>
  <% end %>
  </tbody>
</table>
